addon to jquery.validate

options

 Only numbers:
 
 rules: {
   field: { accept: "[0-9]+" }
 }
 
 Only letters
 
 rules: {
   field: { accept: "[a-zA-Z]+" }
 }


... also includes extra filters for letters only, numbers only or postcodes
